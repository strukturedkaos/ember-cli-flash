{{#if template}}
  {{yield}}
{{else}}
  <div {{bind-attr class=":alert flash.flashType"}}>
    <div class="alert alert-info">
      <button type="button" class="close" data-dismiss="alert">×</button>
      {{flash.message}}
    </div>
  </div>
{{/if}}
